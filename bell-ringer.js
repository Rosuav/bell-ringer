function fix() {
	//TODO: Fix the tab hover text (probably the document.title)
	//TODO: Fix the tab icon
}

fix();
//TODO: Also call fix() any time it appears that a notification has been
//posted. DOM listener??
